<h1>Admin Invoices</h1>

<%= render 'shared/admin_nav' %><br>
<br>
<% @invoices.each do |invoice| %>
  <ul>
    <div id="invoice-<%= invoice.id %>">
      <li>Invoice #<%= link_to "#{invoice.id}", admin_invoice_path(id: invoice.id) %></li>
    </div>
  </ul>
<% end %>
