<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Little Esty Shop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/">Home
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= merchants_path %>"">Merchants</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Admin</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="<%= admin_merchants_path %>">Merchants</a>
            <a class="dropdown-item" href="<%= admin_invoices_path %>">Invoices</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="<%= admin_path %>">Admin Dashboard</a>
          </div>
        </li>
      </ul>
      <form class="d-flex">
        <% if current_user %>
          <%= link_to 'Log Out', logout_path, class: "btn btn-secondary my-2 btn-sm"%>
        <% else %>
          <%= link_to 'Create Account', new_user_path, class: "btn btn-secondary my-2 btn-sm"%>&emsp;
          <%= link_to 'Log In', login_path, class: "btn btn-secondary my-2 btn-sm"%>
        <% end %>
      </form>
    </div>
  </div>
</nav>
